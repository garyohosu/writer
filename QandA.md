# QandA.md
## 仕様不明点リスト

SPEC.md の修正・実装検討中に判明した不明点を記録する。
回答が確定次第、SPEC.md を更新し、本ファイルの該当項目を解決済みに更新すること。

---

### Q1. Plot Agent のタイトル候補3件の選定基準は？
**該当箇所**: SPEC.md §11.1
**内容**: Plot Agent は `title_candidates` として3件を出力する。どれを最終タイトルとして Story Agent に渡すか、選定ロジックが未定義。
**選択肢**:
- (a) Story Agent に3件すべて渡し、Story Agent が選ぶ
- (b) 別途 Title Selection Agent を用意する
- (c) スクリプトがランダム or 先頭1件を固定採用する
- (d) 人間が選ぶ（manual_review モード時）

---

### Q2. Jekyll テーマは何を使うか？
**該当箇所**: SPEC.md §3.1, §16.1
**内容**: MVP では Jekyll 固定としたが、使用テーマが未決定。テーマによってテンプレート構成・AdSense 埋め込み方法が変わる。
**選択肢**:
- (a) Minima（Jekyll デフォルト）
- (b) Chirpy（技術ブログ向け、SEO 強い）
- (c) カスタムテーマ（ゼロから作る）
- (d) その他

---

### Q3. `publication_mode` のデフォルト値は？
**該当箇所**: SPEC.md §25.1
**内容**: MVP 初期は `manual_review` を推奨としているが、config.json のデフォルト値として何を実装に焼き込むか確認が必要。
**選択肢**:
- (a) `manual_review`（推奨通り）
- (b) `automatic`（自動公開前提で始める）

---

### Q4. `banned_terms.json` の初期リストは誰が用意するか？
**該当箇所**: SPEC.md §7, §14.1
**内容**: 禁止事項はプロンプト・レビューで参照されるが、`banned_terms.json` の具体的な初期内容（語句リスト）が未定義。
**必要な確認**:
- 初期リストを手動で作成するか
- 最初は空で運用し、問題発生時に追記するか

---

### Q5. `used_themes.json` の保持期間は何日分か？
**該当箇所**: SPEC.md §13.3
**内容**: 類似度抑制で used_themes.json を参照するが、何日分のテーマを保持・比較対象にするか未定義。
**選択肢**:
- (a) 直近30日
- (b) 直近90日
- (c) 全期間

---

### Q6. レビュー3回失敗時の通知手段は？
**該当箇所**: SPEC.md §12.2
**内容**: 3回失敗で `result: failed` になった場合、人間への通知方法が未定義。ログのみだと気づかない可能性がある。
**選択肢**:
- (a) ログファイルのみ
- (b) Windows 通知（WSL から `powershell.exe -c New-BurntToastNotification` 等）
- (c) メール送信
- (d) その他

---

### Q7. カスタムドメインの有無
**該当箇所**: SPEC.md §16.1
**内容**: カスタムドメインがある場合は優先利用可としているが、実際に使うかどうかで `_config.yml` の `baseurl` / `url` 設定、AdSense 審査ページ URL が変わる。
**必要な確認**:
- 独自ドメインを使うか（使う場合はドメイン名）
- 使わない場合は `garyohosu.github.io/writer` 相当の URL になる

---

### Q8. `stories_index.json` のソート順は？
**該当箇所**: SPEC.md §10
**内容**: 一覧ページでの表示順が未定義。フロントエンド側でソートするか、JSON 自体を常に日付降順で保持するか。
**選択肢**:
- (a) JSON を日付降順で保持（新しい順）
- (b) JSON は追記順で保持し、フロントでソート

---

### Q9. AdSense 広告枠の構成（自動広告 vs 固定枠）
**該当箇所**: SPEC.md §15.2
**内容**: 自動広告と固定枠の使い分けが未決定。両方の併用はレイアウト崩れリスクがある（SPEC.md §15.3）。
**選択肢**:
- (a) 固定枠のみ（記事上部・下部に手動配置）
- (b) 自動広告のみ
- (c) 固定枠を主体にして自動広告を補助的に有効化

---

### Q10. WSL2 上の Python 環境管理方法は？
**該当箇所**: SPEC.md §18.1
**内容**: Python 3.11 以上を前提としているが、仮想環境の管理方法が未定義。cron 実行時にアクティベーション漏れが起きやすい。
**選択肢**:
- (a) `venv`（シンプルで標準的）
- (b) `conda` / `miniforge`
- (c) システム Python を直接使う（非推奨）
- (d) `uv`（高速・モダン）
